#include <iostream>
#include <vector>
#include <queue>
using namespace std;

vector<int> vec[2000];
queue<int> bfs;
int check[100001];

void startBFS(int to, int depth) {
    int 
}

int main() {
    int n, k;
    cin >> n >> k;
    vec[0].push_back(n);

    for(int i = 1; i < 50000; i++) {
        startBFS(k, i);
    }

    return 0;
}