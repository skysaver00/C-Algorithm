#include <stdio.h>
int friends[11][11];

int arefriends(int a, int b) {

}

int main() {
    int C;
    scanf("%d", &C);

    while(C--) {
        int n, m;
        scanf("%d %d", &n, &m);
        for(int i = 0; i < m; i++) {
            int a, b;
            scanf("%d %d", &a, &b);
        }
    }
}