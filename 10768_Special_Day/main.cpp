#include <cstdio>

int main() {
    int month, day;
    scanf("%d %d", &month, &day);

    if(month > 2) {
        printf("After");
        return 0;
    } else if(month >= 2) {
        if(day > 18)
    }

    return 0;
}