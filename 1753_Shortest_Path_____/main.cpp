#include <iostream>
#include <vector>
using namespace std;

vector<int> vec[20001];

int main() {
    ios_base :: sync_with_stdio(false);
    cin.tie(NULL);
    cout.tie(NULL);

    int v, e; cin >> v >> e;
    int start; cin >> start;

    for(int i = 0; i < e; i++) {

    }
}
