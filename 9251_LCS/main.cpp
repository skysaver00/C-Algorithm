#include <bits/stdc++.h>
using namespace std;

string str1;
string str2;

int main() {
    cin >> str1;
    cin >> str2;

    string mirStr;
    if(str1.length() < str2.length()) {

    } else {

    }
}