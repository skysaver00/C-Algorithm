#include <iostream>
#include <algorithm>
using namespace std;

int num[10];
int arr[8];

void dfs(int now, int size, int )

int main() {
    int m, n; cin >> m >> n;

    for(int i = 0; i < n; i++)
        cin >> num[i];
    sort(num, num + n);

    dfs()
    return 0;
}