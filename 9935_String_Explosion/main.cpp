#include <iostream>
#include <string>
#include <stack>
using namespace std;
stack<int> st;
string str;
string bomb;
int arr[40];

int main() {
    cin >> str;
    cin >> bomb;

    int len = str.length();
    int bLen = bomb.length();
    for(int i = 0; i < len; i++) {
        for(int j = 0; j < bLen; j++) {

        }
    }

}