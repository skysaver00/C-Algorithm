#include <stdio.h>
#include <queue>
using namespace std;

priority_queue<int> val;

int main() {
    int n;
    for(int i = 0; i < n; i++) {
        for(int j = 0; j < n; j++) {
            int val;
            scanf("%d", &val);
        }
    }

    return 0;
}