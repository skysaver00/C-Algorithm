#include <bits/stdc++.h>
using namespace std;

struct co {
    int x;
    int y;
};

int main() {
    int n;
    cin >> n;

    struct co point[100001];

    for(int i = 0; i < n; i++) {
        cin >> point[i].x >> point[i].y;
    }


    return 0;
}