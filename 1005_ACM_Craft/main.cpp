#include <stdio.h>
#include <vector>
#include <queue>
using namespace std;

int level[1001];
vector<int> path[1001];
queue<int> topoQueue;

int main() {
    int t;
    scanf("%d", &t);

    while(t--) {
        int n, k;
        scanf("%d %d", &n, &k);

        
    }
    return 0;
}