#include <iostream>
#include <vector>
using namespace std;

vector<pair<int, int>> vec;
int dist[501];

int main() {
    int tc; cin >> tc;
    while(tc--) {
        int n, m, w; cin >> n >> m >> w;
        for(int i = 0; i < m + w; i++) {
            int s, e, t; cin >> s >> e >> t;
            
        }
    }
}