#include <stdio.h>
#include <deque>
using namespace std;
int arr[1001];

int dfs() {

}

int main() {
    int t;
    scanf("%d", &t);

    while(t--) {
        int n;
        scanf("%d", &n);
        
    }
}