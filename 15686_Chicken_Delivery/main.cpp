#include <stdio.h>
int mp[51][51];
int t[13][101];

int main() {
    int n, m;
    scanf("%d %d", &n, &m);

    for(int i = 0; i < n; i++) {
        for(int j = 0; j < n; j++) {
            scanf("%d", &mp);
        }
    }

    for(int i = 0; i < n; i++) {
        for(int j = 0; j < n; j++) {
            if(mp[i][j] == 2) {
                for(int k = 0; k < n; k++) {
                    for(int h = 0; h < n; h++) {
                        
                    }
                }
            }
        }
    }
}