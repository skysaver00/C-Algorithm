#include <bits/stdc++.h>
using namespace std;

int main() {
    int n;
    cin >> n;

    priority_queue<int, vector<int>, greater<int>> cards;

    for(int i = 0; i < n; i++) {
        int t;

        cin >> t;
        cards.push(t);
    }

    long long sum = 0;

    while(!cards.empty())

    return 0;
}